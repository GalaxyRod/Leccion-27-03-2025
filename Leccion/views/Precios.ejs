<!DOCTYPE html>
<html lang="es">
<head>
    <title>Servicio</title>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>

  <%- include('partials/header') %>
  <%- include('partials/menu') %>

  <form id="rateForm">
    <label for="lower">Minimum Rating:</label>
    <input type="number" id="lower" name="lower" required>
    <label for="higher">Maximum Rating:</label>
    <input type="number" id="higher" name="higher" required>
    <button type="submit">Search</button>
</form>

<div class="container-fluid p-4">
    <h1 class="pb-4"><%= title %></h1>
    <table class="table table-striped table-hover">
      <thead>
      <tr>
          <th>Id</th>
          <th>Nombre</th>
          <th>Descripción</th>
          <th>Precio</th>
          <th>Supervisor</th>
      </tr>
      </thead>
      <tbody>
          <% arrServ.forEach((servicio) => { %>
              <tr>
              <td><%= servicio.id %></td>
              <td><%= servicio.nombre %></td>
              <td><%= servicio.descricpion %></td>
              <td><%= servicio.precio %></td>
              <td><%= servicio.supervisor %></td>
              </tr>
          <% }); %>
      </tbody>
  </table>
</div>

  <%- include('partials/footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>